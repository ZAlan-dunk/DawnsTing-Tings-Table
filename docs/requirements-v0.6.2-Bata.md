# 懒羊羊当大厨～ v0.6.2-Bata 原生视觉回填

## 版本与边界

- `versionCode 9` / `versionName 0.6.2-Bata`
- 包名与命名空间保持 `com.dawns.tingstable`。
- 保持现有本地数据、菜谱搜索筛选、菜篮、特典和采购清单兼容。
- 不增加账号、联网、分析、广告或 `INTERNET` 权限。

## 首页

- Hero 使用用户确认的图片资源，提供淡色与黑夜两套 1440×810 位图。
- Hero 使用浅色表面、细边界和深色文字，不再使用大面积深绿色背景。
- 保留“漂亮嘞女明星～”、菜篮数量、可制作数量和推荐入口。
- 四个主要入口使用双列统一卡片、同一图标底色和可见边框。

## 控件与皮肤

- 顶部栏、入口卡片、皮肤按钮、菜谱搜索/筛选图标和底部导航使用统一控制 token。
- 卡片标题、说明文字和选中态满足可读性要求，交互目标不小于 48dp。
- 淡色/黑夜选择继续持久化，系统关闭动画时保留静态反馈。

## 验收

- 单元测试、Debug/Release Lint、Debug/Release 构建通过。
- Release APK 使用连续测试签名并通过 `apksigner`。
- APK 元数据为版本 9 / 0.6.2-Bata，且没有网络权限。
- 旧 GitHub Releases 和安装包保持不变，新建 `v0.6.2-Bata` Pre-release。
