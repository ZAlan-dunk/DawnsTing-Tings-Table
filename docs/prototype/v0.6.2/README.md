# v0.6.2 Web 视觉原型

这是下一轮 APK 视觉方向的独立原型，不会改变当前安装包。

打开 `index.html` 即可查看；页面内可切换首页/菜谱和淡色/黑夜皮肤。原型刻意不使用网络图片、渐变背景或多色卡片，用来先确认“轻、少、静”的方向。

当前环境未执行屏幕截图或 GUI 自动化。确认视觉方向后，再将确认后的 token 和布局回填 Android，并进入下一版 `v0.6.2-Bata` 构建。
